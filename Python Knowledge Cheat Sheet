1. create empty set:
  use a = set() rather than a ={} --- this will be a list

2. create a list of sets
  c = [set() for i in range(9) ]  vs. c = [set()] * 9
  
3. item.copy() vs item.deepcopy()




Some strategies for avoid overfitting? What's their advantages and disadvantages?
Implement the pseudo code of SkipGram algorithm
Some methods to extend binary classifier to multi-way classifier? advantages vs. disadvantages?
What's gradient vanishing/explosion? How does LSTM solve this problem?
